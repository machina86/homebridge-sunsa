{
  "pluginAlias": "Sunsa",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "apiKey": {
        "title": "API Key",
        "description": "Get API Key from sunsa app Settings->API Settings",
        "type": "string",
        "required": true
      },
      "idUser": {
        "title": "User ID",
        "description": "Get idUser from sunsa app Settings->API Settings",
        "type": "integer",
        "required": true
      },
      "showTemp": {
        "title": "Show Temperature Sensors",
        "description": "Expose temperature sensors. (default: true)",
        "type": "boolean",
        "default": true,
        "required": false
      },
      "polling": {
        "title": "Polling",
        "description": "Set polling time for wands in seconds. (default: 10)",
        "type": "integer",
        "default": 10,
        "required": false
      }
    }
  }
}